// Stub file for @p-stream/providers
// This is a private package from P-Stream that needs to be replaced with Zenflify API integration

export interface ProviderControls {
  // Add provider control types as needed
}

export interface SourcererOutput {
  // Add source output types as needed
}

export interface RunOutput {
  // Add run output types as needed
}

// Add other necessary exports as compilation errors appear
export const providers = [];
export const targets = [];
export const features = [];
